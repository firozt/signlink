import { StyleSheet } from 'react-native';

// File containing global styles that are used throughout the codebase

const GlobalStyles = StyleSheet.create({
  text: {
    fontSize: 16,
    color: 'white',
    fontFamily:'roboto',
  },
  heading1: {
    fontSize: 64,
    color: 'white',
    fontFamily:'Roboto',

  },
  container: {
  }
  
});

export default GlobalStyles;
